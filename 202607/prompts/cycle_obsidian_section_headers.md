---
plan: 202607/cycle_obsidian_section_headers.md
---
 Can you help me make the `<ctrl+j>` and `<ctrl+k>` keymaps in Obsidian, which jump to the next/previous section header in the current note file cycle? In other words, when there are no sections above the cursor but the user presses `<ctrl+k>` we should navigate to the very last section in that file (and vice-versa for the `<ctrl+j>` keymap). Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the
`sase plan propose` command (as the skill instructs) before making any file changes.
 