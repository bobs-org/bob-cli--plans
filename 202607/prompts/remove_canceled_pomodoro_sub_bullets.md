---
plan: 202607/remove_canceled_pomodoro_sub_bullets.md
---
 We recently improved a bob command so that it removes canceled task block links from daily file open Pomodoros. It looks like the command just removes the block link though. It should remove the entire sub-bullet. Can you help me fix this? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
