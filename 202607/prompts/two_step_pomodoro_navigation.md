---
plan: 202607/two_step_pomodoro_navigation.md
---
 The `<ctrl+9>` keymap currently jumps to either the first open pomodoro in the current file or (if the current file contains no open pomodoros) opens today's daily file and moves the cursor to the first open pomodoro in that file. Can you help me make it so, in the 2nd case, the first press of `<ctrl+9>` just opens the daily file with the page's scroll position and cursor position unchanged (if we opened this file before in this Obsidian session--see how we do this for the `<ctrl+0>` keymap for inspiration)? The 2nd press of `<ctrl+9>` should trigger the same jump to the first open pomodoro in that file that this keymap triggered before. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
