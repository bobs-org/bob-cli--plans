---
plan: 202607/preserve_move_only_task_status.md
---
 Can you help me make it so, when a block link to a task has a `#` suffix, the `<ctrl+enter>` Obsidian keymap does not mark that task as in-progress? We already correctly removed that block length from the current Pomodoro before completing it but it seems like we still mark the corresponding task as in-progress, which is incorrect. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
