---
plan: 202607/counted_obsidian_task_properties.md
---
 Can you help me start allowing the `<ctrl+shift+p>` Obsidian keymap to accept an optional count that indicates that the next `<N>` tasks should have their dataview properties edited as well? For example, if my cursor is on the `^read-sase-beads` task in the ~/bob/sase.md file and I press `2<ctrl+shift+p>` and then select `scheduled` and pick a date 1 week from today, then the `^read-sase-beads`, `^fix-fix-just`, and `^fix-gh-act-and-pub` tasks should all have the `scheduled` property added/modified and set to the selected date (1 week from today).

Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 