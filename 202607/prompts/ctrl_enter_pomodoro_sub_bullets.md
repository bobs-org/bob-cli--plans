---
plan: 202607/ctrl_enter_pomodoro_sub_bullets.md
---
 Can you help me make it so the `<ctrl+enter>` Obsidian keymap uses a special branch of logic when the current line is any sub-bullet of a pomodoro in one of my daily files except for a transcluded block link to an Obsidian task (in that case, we should close the task as we do currently)? Namely, in this case we should act as if `<ctrl+enter>` was pressed while the cursor was on the pomodoro line (with the checkbox). This way, the user can close the current pomodoro and (if any tasks rolled over) create the next pomodoro without having to move the cursor from the sub-bullet to the pomodoro headline.

Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 