---
plan: 202607/task_move_spacing_and_picker_close.md
---
 The `<ctrl+shift+m>` keymap we added yesterday seems to always leave a blank line in-between tasks when moving them. Also, the menu should disappear after selecting the file to move the selected task(s) to. Can you help me fix these issues? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
