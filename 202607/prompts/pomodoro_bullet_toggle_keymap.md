- **PLAN:** [../202607/pomodoro_bullet_toggle_keymap.md](../pomodoro_bullet_toggle_keymap.md)

 Can you help me add a new `<ctrl+alt+]>` keymap that is only active in Obsidian daily files that converts an empty sub-bullet to a pomodoro note bullet (i.e. converts `  - ` into `- [ ] ()`? Make sure this keymap supports the reverse operation as well (i.e. converts an empty/open pomodoro into a sub-bullet). Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 