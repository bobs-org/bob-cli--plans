---
plan: 202607/counted_task_moves.md
---
 #fork:a7 Can you now help me add a new `<ctrl+shift+m>` keymap that also accepts an optional count (which also targets the next `<N>` tasks as well as the current task if specified)?

- This keymap will be used to move one or more Obsidian tasks from one file to another.
- When this keymap is used the user should be prompted to select an existing area / open project note file. Make sure the type of the note file (area or project) and the status (if a project) is illustrated in the menu (see how we do this elsewhere and re-use that logic).

Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 