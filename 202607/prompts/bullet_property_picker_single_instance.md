---
plan: 202607/bullet_property_picker_single_instance.md
---
 We recently fixed an issue where the `<ctrl+shift+m>` keymap was showing a duplicate menu. We seem to be having the same issue with the `<ctrl+shift+p>` keymap (I know it happens when a count is provided at least). Can you help me diagnose the root cause of this issue and fix it? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 