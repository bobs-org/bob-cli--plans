- **PLAN:** [../202607/universal_task_picker_block_links.md](../universal_task_picker_block_links.md)

 Can you help me make the special `^^` pop-up that prompts for an open task work on any block link instead of just on block links in sub-bullets of tasks/pomodoros? We should still only update the task's status to `[*]` if the block link is the only contents of a pomodoro sub-bullet. Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
 