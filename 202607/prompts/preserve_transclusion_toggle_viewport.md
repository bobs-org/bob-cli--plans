---
plan: 202607/preserve_transclusion_toggle_viewport.md
---
 Every time I use the `!` keymap in Obsidian, it the page is scrolled such that the currently selected line is positioned at the bottom of the screen (so I can't see what I was looking at 10 lines below my current line anymore, for example). Can you help me diagnose the root cause of this issue and fix it? Think this through thoroughly and create a plan using your `/sase_plan` skill. Choose and author the appropriate
tier, validate and revalidate until it passes, then submit it with `sase plan propose` (as the skill instructs)
before making any file changes.
